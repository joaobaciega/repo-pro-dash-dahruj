streamlit>=1.30
pandas>=2.0
plotly>=5.18
openpyxl>=3.1
SQLAlchemy>=2.0
PyMySQL>=1.1

# Trava temporária: starlette 1.4.0 (05/08/2026) tornou obrigatório o parâmetro
# `thread_minimum_size` no GZipResponder e quebra o middleware gzip do Streamlit
# 1.61.0 -> TypeError em toda requisição, app responde 500. Remover quando sair
# um release de Streamlit > 1.61.0 com a correção.
starlette<1.4
